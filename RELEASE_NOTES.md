保存之后马上「文件 ▸ 新建」，新画的这张一帧都录不上，之后怎么画都没反应；另一种表现是新画的帧被录进了上一张的文件夹。根因是文档切换恰好落在一次文档同步等 Photoshop 回话的窗口里，旧文档的答复把「当前文档」改了回去。

## 是怎么发生的

插件靠 `getDocumentInfo` 确认当前文档的路径和画布。保存会发出一次这样的请求，而 Photoshop 要等文件写完才回话。这段时间里用户新建了文档：插件收到「当前文档变了」的事件，把当前文档记成新的；然后旧文档的答复到了，旧代码把它当作当前文档的答复来用，「当前文档」于是又被改回旧的那张。

从这一刻起，新文档上的每一笔都因为「不是当前文档」被扔掉。心跳也救不回来：心跳问的是它以为在前台的那张，而这个「以为」正是被改错的那个。要到用户再切换一次文档才恢复。

同一个竞态还有另一面。答复已经回来、插件正在为旧文档解析会话（读写 PSD 里的 id，保存后这一步同样慢）时切换文档，解析结束后会把结果记成「新文档已经解析过了」，新文档随后就直接录进旧文档的文件夹 —— 这是「串」的另一条路径。

## 现在会怎样

- 每次同步绑定它出发时的那个文档。Photoshop 回话时、分支或解析完成时，各核对一次前台文档还是不是它；不是就丢掉这次的结果，立刻为新的前台文档再同步一次，日志里写一行。
- 「解析给了哪个文档」记的是实际解析的那个，不再是解析结束时恰好在前台的那个。
- 面板上的「采用会话」「新建会话」「删除当前录制」在等 Photoshop 期间切换了文档，也不会把准备好的会话装到新文档上，改为触发一次同步。
- 文档切换后、同步完成前落下的第一笔，现在会在同步完成时立刻抓下来，不用等第二笔。

被丢掉的那次同步解析出的会话不会丢：它已经记在文档映射和索引里，切回那张文档时照常接上。

## 测试

183 条，全绿，类型检查通过。新增 `test/new-document.test.mjs`，用一个有多份文档的假 Photoshop 驱动真实构建包：保存后 Photoshop 迟迟不回话期间新建文档、回话后正在解析会话期间新建文档，两种情况新文档都必须在 3 秒内（比心跳短）录进自己的文件夹，旧文档的文件夹不能多一帧，切回旧文档要接着录进原文件夹并补写它的 id；新文档同步完成前落下的第一笔必须被录下。这三条在旧代码上确认失败；Photoshop 立即回话的那条本来就过，留作基线。调度器加一条：disabled 期间报告的变化在 enable 时立刻抓。

## 安装

解压后跑 `scripts\install.ps1`。已经装过旧版的直接覆盖即可，已有的录制和过程图都不受影响。
