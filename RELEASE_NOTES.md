一个能把整段录制毁掉的补白 bug。根因在 Photoshop 那侧，用只读探针插件在 Photoshop 2026 上实测确认了，顺带把抓帧的几何算法整个换掉。

## 每一帧都被补成一整幅白纸，画缩在左边

8000×2000 的画布，录了 169 帧，每一张都是 8000×2094：画挤在最左边 2880 像素里，右边 5120 列纯白 —— 从第一帧到最后一帧，无一幸免。

这份文档做过 Image Size、改过分辨率、又用 Canvas Size 横向拉长，图层早就伸到画布外面去了。这一点从 PSD 的图层记录里能直接读出来：有一层是 `3685,-2246 -> 8322,4246`，上下各超出画布 2246 像素。而插件请求的 `clipToDocumentBounds: true` **并没有把它们裁掉**，Photoshop 回来的是图层并集缩放后的 2880×2247 —— 比画布还高。

原来的代码只能靠 `pixmap.bounds` 和实际像素宽度一比去反推缩放比。Photoshop 这时把 bounds 报成了缩放后的坐标（`0,0 -> 2880,2247`），比值于是读成 1，右边就补出了 5120 列白。

## 根因：三个参数缺一不可

写了个只读的探针 Generator 插件，在 Photoshop 2026 上拿同一份文档试各种请求组合：

| 请求 | 返回 | bounds |
|---|---|---|
| `clip + maxDimension`（4.2.1 的写法） | 2880×2247 | `0,0 -> 2880,2247` |
| `inputRect/outputRect + maxDimension`（3.x 的写法） | 2880×2247 | `0,0 -> 2880,2247` |
| 只给 `outputRect`，不给 `inputRect` | 8322×6492 | 请求被整个忽略 |
| **`inputRect + outputRect + clip`** | **2880×720** | **`0,0 -> 2880,720`** |

结论很干脆：**请求里只要带 `maxDimension`，`clipToDocumentBounds` 就会被静默忽略；`outputRect` 不配 `inputRect` 也会被忽略。** 三个一起给，Photoshop 才真的裁到画布，并按指定尺寸渲染。

顺带否掉一个看似合理的猜测：3.x 那套 `inputRect`/`outputRect` 并不能避开这个坑 —— 它一样带着 `maxDimension`，实测结果和 4.2.1 一模一样。

## 改法：不再反推缩放比

抓帧改成先用 `computeOutputRect()` 算出一个明确的矩形，连同 `inputRect` 和 `clipToDocumentBounds` 一起发过去。于是：

- 缩放比是**我们指定的**，不是猜出来的；
- Photoshop 把返回的像素报在同一套坐标里，补白退化成对着这个矩形做减法；
- 画满整幅画布时补白全为 0；只画了一角时也落在正确位置（实测：4000×3000 画布上 200,150 处一块 400×300，按 1663×1247 请求，回来 167×126、bounds `83,62 -> 250,188`，补白 `83/62/1413/1059`，正好凑满 1663×1247）。

`isScaledWholeDocument` 和整段缩放比反推逻辑随之删掉 —— 这个 bug 的脆弱内核不是又加了一层保护，而是不复存在了。

保留一道防御：**Photoshop 返回的东西如果比请求的矩形还大，就说明它没听我们的**，这时宁可不补白、原样写出去。老版本 Photoshop（2020–2025）没条件实测，这道防御保证最坏情况也只是不补白，不会再出现这次这种废帧。日志里另有一行警告，写明请求了多大、回了多大，一段录制只报一次。

## 已经录坏的过程还能救

坏帧里的画一点没丢，只是被白边围住了。源码仓库里新增了 `scripts/repair-frames.mjs`，把白边量出来剪掉。它要用到 jpeg-js，所以从仓库跑（先 `npm install`），不在安装包里：

```
node scripts\repair-frames.mjs "<过程图文件夹>"
```

原文件夹一个字节都不动，结果写到旁边的 `<文件夹名>-repaired`，`session.json` 一并带过去（画布尺寸改写成裁剪后的真实尺寸）。把它放进过程图目录，面板的「录像」页就能看到，照常导出。

补白是编码器写出来的纯 255 白，真实的画经过 JPEG 不可能留下这么大一整块死白，所以量得准。同一尺寸的帧统一按其中最大的内容框裁，避免逐帧松紧不一导致画面抖动。

要注意剪出来的是 Photoshop 当时真给的那块区域（图层并集），不是画布。想精确还原到画布，还得知道画布在那块区域里的偏移 —— 这个偏移只能从对应 PSD 的图层记录反推，脚本没法自动做。

## 测试

174 条，全绿。`test/framing.test.mjs` 整个重写，用的全是探针在真 Photoshop 上量到的数字；`test/downscale.test.mjs` 现在同时驱动三种 Photoshop —— 一种守规矩的、两种不守规矩的 —— 确认后两者最坏也只是帧不补白。类型检查通过。

## 安装

解压后跑 `scripts\install.ps1`。已经装过旧版的直接覆盖即可，已有的录制和过程图都不受影响。
