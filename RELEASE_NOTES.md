一次静默失录：另存为、改画布之后，新文件一帧都没录上，日志里一个字都没有。根因是 Photoshop 有 89 分钟不回应任何脚本请求，而插件在这种情况下既不会说、也恢复不了。顺着这条线又挖出六个同类问题，一起修了。

## 事故是怎么发生的

一份 150 MB 的 PSD 放在 OneDrive 同步目录里。按下保存的那一秒抓到了最后一帧，之后 Photoshop 对 Generator 的脚本请求一条都不再回应，直到 89 分钟后下一次保存完成才把攒下的请求一口气答完。这期间 `imageChanged` 事件照常推送：文件改名了（另存为）、画布改了两次尺寸、落了 27 分钟的笔，插件全都收到了，但用来确认这些变化的 `getDocumentInfo` 一直挂着。

旧代码在这种状态下有三个问题叠在一起：

- 有一次文档同步在飞时，另存为、改画布、切换文档触发的同步请求**直接丢弃**，等它回来了也没有人再跑一次。
- 画布尺寸和已同步的不一致时，抓帧静默返回，不主动要求同步，只等 5 秒一次的心跳 —— 而心跳发出的请求同样挂着。
- 脚本请求没有任何超时。一条不回来，整个同步路径就永远卡死，日志和面板上什么都没有。

于是：另存为没有分支，画布改了没同步，之后每一笔都被「画布变了，先别抓」挡掉，全部静默。Photoshop 终于回话那一刻分支才发生，三分钟后 Photoshop 被关掉了。

要说清楚的一点：卡住期间 pixmap 请求走的是同一条通道，任何插件逻辑都录不到那 27 分钟。插件欠的是把这件事喊出来，以及 Photoshop 一恢复就立刻接上。

## 现在会怎样

- 同步在飞时收到的请求记下来，前一次一结束立刻再跑一次。另存为和改画布在 Photoshop 回话后的第一时间就被处理，不再等心跳。
- 抓帧被旧画布挡住时主动要求同步。
- 一条请求 **60 秒**没回话：面板弹出错误提示「Photoshop 没有回应」，同时重新发一次请求；旧请求的答复如果之后到了会被丢弃，只有最新的一次同步能改状态，分支和解析不会被并发跑两遍。同一段卡住只报一次，Photoshop 恢复时写一条日志说明卡了多久。
- 卡住期间超时的抓帧攒够五次会自动暂停，这是原有行为；现在 Photoshop 恢复回话时会自动解除这个暂停，不用再手动点。用户自己点的暂停和导出时的暂停不受影响。
- 因 `getDocumentInfo` 失败而脱离文档时写日志，不再一声不吭。

## 顺手修掉的同类问题

- **另存为修复和分支的竞态。** 保存事件会触发一次「读 PSD 里的 id，没有就写回去」的修复。这次事故里这个读被卡了 89 分钟，期间分支已经把新 id 写进文档；如果 Photoshop 真把设置清了，旧代码会在读回来之后把**旧 id** 写回去盖掉新的，之后新文件的每一帧都会进旧文件的文件夹。这才是真正会「串」的路径。现在分支、新建、采用会话都先在内存里认领文档再写 PSD，修复逻辑写之前重新核对归属，归属变了就退出。
- **待补写的 session id 每秒发一次。** 没有进行中的保护，Photoshop 卡 89 分钟会攒下五千多条写请求，醒来一口气执行。现在一次只发一条。
- **启动顺序。** 原来先等版本查询和菜单安装回来才订阅事件、起心跳，Photoshop 启动时慢一点就整个不工作。现在先订阅、先起心跳。
- **空 pixmap 静默返回。** Photoshop 返回空像素时一个字不写，和「根本没请求」看起来一样。现在每个 session 记一次。
- **平常保存一次会丢掉最后一帧的时间戳。** 解析会话时会从磁盘重读 manifest，而磁盘上的是两秒前的版本。现在解析前先把内存里的落盘。

## 使用上的建议

- 正在画的 PSD 不要放在 OneDrive 之类的同步目录里，同步客户端会抓着刚写完的文件不放。
- 可以试试关掉「首选项 ▸ 文件处理 ▸ 在后台存储」，看还卡不卡。这只是从这一次事故推测的最可能机制，没有实测确认。
- 看到面板报「Photoshop 没有回应」时，先按一次 Ctrl+S。这次事故里正是下一次保存让 Photoshop 把攒下的请求答完的。不行就重启 Photoshop，重开之后会接着录进原来的文件夹。

## 测试

178 条，全绿，类型检查通过。新增 `test/saveas-busy.test.mjs`，驱动真实构建包：同步在飞期间发生另存为、改画布、落笔，Photoshop 回话后 3 秒内必须在副本上抓到帧（3 秒比心跳短，旧代码过不了）；一条请求永远不回时必须报一次错、重发、恢复抓帧。另存为修复和分支的竞态、调度器自动暂停和手动暂停的区分也各有一条，前者确认了不修会失败。

## 安装

解压后跑 `scripts\install.ps1`。已经装过旧版的直接覆盖即可，已有的录制和过程图都不受影响。
