两个导出相关的修复:录制途中导出不再报错，弹出来的错误提示也终于能关掉了。

## 导出「当前正在录制」的这一段会失败

导出正在录制的这段时，ffmpeg 报 `No such file or directory`，指着一张刚刚才写好的 `final.jpg`。从「录像」页导出以前的旧录制不受影响 —— 只有当前这段会中招。

根因是导出临时目录「自己删掉了自己的输入」：

- 面板先把收尾静帧写进 `exportTemp\final.jpg`，再把这个路径交给导出器当片头片尾
- 导出器确认文件在，于是决定这次要带片头片尾
- **紧接着它又「为了防上次残留」把整个 `exportTemp` 清空了一遍 —— 连刚写进去的 `final.jpg` 一起删了**
- 但「要用它」这个决定已经下过了，于是照样把一个已经不存在的路径喂给 ffmpeg

面板和导出器共用同一个临时目录，却各自以为「清空它是我的事」。修复是去掉那次多余的预清空：帧清单每次都是整体重写、导出成功后本来就会清理整个目录，预清空除了误删输入之外没有任何作用。

## 错误提示弹出来就关不掉

导出失败这类错误提示，设计上是「一直留着，直到你手动关」—— 但提示组件只在带一个可点操作按钮（比如「打开」）时才画出那个按钮。导出失败的提示没有操作按钮，于是它既不会自动消失、也没有任何可以点的地方，只能一直挂在面板底下。

现在给所有这类没有操作按钮的提示补上一个 `✕` 关闭按钮，随时点掉即可。十种界面语言都加了对应文案。

## 测试

169 条，全绿。这两处修复分别落在导出流程（`runExport`）和提示组件里，纯函数与导出参数构建的既有单测不受影响；类型检查通过。

## 安装

解压后跑 `scripts\install.ps1`。已经装过旧版的直接覆盖即可，已有的录制和过程图都不受影响。
