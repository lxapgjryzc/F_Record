F_Record 重开一版。功能是原来那套已经长齐的功能，代码是重新收拾过的代码：整个仓库的每一行、每一个分支、每一个函数都被测试跑到过，路上挖出来的两个真 bug 也一并修了。

## 它能做什么

在 Photoshop 里挂着，你画你的，它按固定间隔把画布存成一帧一帧的图，最后能拼成一段过程视频。

- **录制**：自动跟着当前文档走，换文档、另存为、关掉再打开都认得出是同一件作品。
- **导出**：可以按目标时长压缩（真的会变短，不是掐掉后面），可以选画幅比例，可以在视频上盖水印 —— 文字或图片，压在角落或者满屏浮雕。
- **复制画面**：把当前画布签好名直接送进剪贴板，随手发给人看。盖不盖水印在设置里定一次。
- **整理**：所有录制都列在面板里，可以归档、打包成 zip、按「多久没动」「帧数太少」批量挑出来处理；「清理」会把每件作品的原文件一个一个在 Photoshop 里打开给你看，当面决定留还是删 —— 删除动作全部攒到最后，一次确认，中途退出不损失任何东西。

## 这版做了什么

**测试补齐了。** 944 条测试，35 个模块，行/分支/函数三项全部 100%，`npm run check` 会把这个门槛卡住。生成器、共享代码、ffmpeg 那一层，还有面板那三千行 preact 界面 —— 界面是靠一个手写的 DOM 垫片跑起来的，测试点的是真渲染出来的按钮，所以它验的是「用起来是什么样」，不是「函数被调过没有」。仓库没有为此增加任何依赖。

**顺带修掉两个真 bug。**

- 找 ffmpeg 的备选路径里，`"C:\ffmpeg\bin"` 少写了一层反斜杠，编译出来是换页符加退格符 —— 那条路径永远匹配不上，注释里明明写着它该管用。
- `Bridge.stop()` 等 `server.close()` 返回，而只要还有一条连接开着它就不会返回（面板挂着的事件流、http agent 复用的 keep-alive socket 都算）。**Photoshop 退出时会卡在插件这儿。** 现在它记住每条连接，停的时候一并拆掉。

**盖章那条路也稳了。** 录制 id 写进 PSD 时会先确认写的是不是当前那个文档，不再有写串的机会。

## 装法

解压，用管理员身份跑 `install.cmd`。装完重启 Photoshop，在「窗口 → 扩展功能」里找 F_Record。

卸载跑 `uninstall.cmd`。装的时候会自动下载 ffmpeg（约 138 MB），第一次装要联网。

> 面板和生成器是两半，**必须一起装**。这版的协议号是 12。
