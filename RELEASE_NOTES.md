4.9.0 的「复制画面」是一定盖水印的。这版把它变成可选：设置里水印那一栏底下多了一个**「复制画面时加水印」**开关，关掉就是复制原图，导出视频照旧盖。

## 盖不盖，在设置里定

开关就在水印的样式、大小、不透明度下面 —— 它调的是同一件事，只是多问一句「复制画面的时候算不算」。默认是**开**：把水印设起来的人，本来就是想让递出去的东西带着它。

水印本身设成「关」的时候这个开关不显示，因为没有可加的东西。这和上面那几行的规矩一致：不再有意义的选项就不占位置。

**为什么是设置，不是每次问一句。** 这个按钮的意义就在于一次点击。导出是个郑重的动作，本来就有对话框可以问，所以那里可以临时换文字、换图、这一次不盖；而画到一半复制一张发给朋友看，是个随手的动作，答案基本每次都一样 —— 每次弹个框问，等于把一次点击变成两次，换来的是一个几乎从不改的选择。

## 细节

- 鼠标停在「复制画面」上的提示会跟着变：真的会盖才说「盖上水印」，不盖（开关关了，或者水印是「文字」但还没填字）就只说复制。判断用的是导出那边判断「这个水印到底画不画得出东西」的同一个函数，所以提示不会承诺一个不会出现的签名。
- 老配置里没有这一项，按**开**处理 —— 只有明明白白写着 `false` 才算关。手改成 `"no"` 之类的字符串也是开，因为那不是 `false`。
- 面板和生成器的**协议号升到 12，两边都要重装**。老的生成器其实会把这个字段原样带过去（它只覆盖自己认识的键），开关是能存住的；升号是因为它不会去规范这个值，而配置文件归生成器管。与其对每个新字段单独判断「旧的那半是不是刚好无害」，不如直接说清楚「这两半不是一个版本」。
