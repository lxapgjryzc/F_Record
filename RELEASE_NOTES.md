F_Record 4.0.1。接着 4.0.0 那条线走：功能是原来那套已经长齐的功能，代码是重新收拾过的代码 —— 整个仓库的每一行、每一个分支、每一个函数都被测试跑到过。

## 它能做什么

在 Photoshop 里挂着，你画你的，它按固定间隔把画布存成一帧一帧的图，最后能拼成一段过程视频。

- **录制**：自动跟着当前文档走，换文档、另存为、关掉再打开都认得出是同一件作品。
- **导出**：可以按目标时长压缩（真的会变短，不是掐掉后面），可以选画幅比例，可以在视频上盖水印 —— 文字或图片，压在角落或者满屏浮雕。
- **复制画面**：把当前画布签好名直接送进剪贴板，随手发给人看。默认缩到 1080p —— 口径和录制分辨率一样，要原图在设置里选；盖不盖水印也在设置里定一次。
- **整理**：所有录制都列在面板里，可以归档、打包成 zip、按「多久没动」「帧数太少」批量挑出来处理；「清理」会把每件作品的原文件一个一个在 Photoshop 里打开给你看，当面决定留还是删 —— 删除动作全部攒到最后，一次确认，中途退出不损失任何东西。

## 这版做了什么

**「复制画面」不再默认给原图。** 这个按钮是拿来随手发进度的，而剪贴板里放的是没压缩的位图：一张 6000 像素的画布就是上百 MB，粘到哪个聊天窗口都要卡一下。JPEG 质量那种压缩在剪贴板上不起作用，能让它变小的只有像素数。所以设置里多了一项「复制画面的分辨率」，口径和录制分辨率完全一样：像素数大约等于这个高度的 16:9 一帧，画布本来就更小的照原样复制。默认 1080p，复制出来的就是一帧 1080p 录像那么大；要全尺寸就选「原图」。缩放在盖水印之前做，水印按缩完的图算大小，和成片上的比例一致。

**按文件名认领录制的那条路收紧了。** 认回一件作品有三处依据：PSD 里的 id、这次运行的文档表、恢复索引。前两个记的是「哪个文档」，索引记的是「哪个文件」—— 而文件名比画重复得多：新画一张存成旧名字、资源管理器里把一个 PSD 盖到另一个上面，路径都没变，画已经不是那幅画了。现在索引只在两件事都成立时才算数：文档是从那个文件**打开**的（刚另存到这个路径上的是一件新作品），而且画布尺寸和那段录制最后一次见到的一样。PSD 自己带的 id 也不会再被索引推翻 —— id 是跟着像素走的，文件被复制、改名到哪儿，里面装的还是那幅画；4.0.0 里那条反过来拿索引去改文档 id 的补救，因此整条去掉了。

**底子没变。** 965 条测试，36 个模块，行/分支/函数三项全部 100%，`npm run check` 卡着这个门槛。这一版还在真的 Photoshop 2026 里从头跑过一遍：复制出来的图和同一张画布的录制帧一个像素不差，上面那几种认文件的情形也逐个验过 —— 包括「另存成旧名字不被认领」和「尺寸对不上就不认」。

## 装法

解压，用管理员身份跑 `install.cmd`。装完重启 Photoshop，在「窗口 → 扩展功能」里找 F_Record。

卸载跑 `uninstall.cmd`。装的时候会自动下载 ffmpeg（约 138 MB），第一次装要联网。

> 面板和生成器是两半，**必须一起装**。这版的协议号是 13。
